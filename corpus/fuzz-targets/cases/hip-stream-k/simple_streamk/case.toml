name = "hip_streamk_simple"
project = "hip-stream-k"
runner = "runner.cpp"
upstream_repo = "https://github.com/ROCm/hip-stream-k"
source = "../../../third_party/hip-stream-k"
default_variant = "upstream"

[input]
description = "Upstream simple_streamk F32 GEMM example; accepts -m, -n, -k, -g, --num_runs, --validate, and -v."

[validation]
expect = "pass"
tolerance = "runtime --epsilon option, default 1.0e-2"
